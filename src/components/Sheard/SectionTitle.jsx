

const SectionTitle = ({ title }) => {
    return (
        <div className="my-6">
            <h2 className="text-4xl font-bold border-b-2 py-3 text-center uppercase">{title}</h2>
        </div>
    );
};

export default SectionTitle;